/**
 * White Label Communications CPaas API Documentation
 * A CPaaS platform API
 *
 * The version of the OpenAPI document: 1.1
 * Contact: support@whitelabelcomm.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ServiceWebhookAdd model module.
 * @module model/ServiceWebhookAdd
 * @version 1.1
 */
class ServiceWebhookAdd {
    /**
     * Constructs a new <code>ServiceWebhookAdd</code>.
     * @alias module:model/ServiceWebhookAdd
     * @param callbackUrl {String} 
     * @param title {String} 
     * @param webhookType {module:model/ServiceWebhookAdd.WebhookTypeEnum} 
     */
    constructor(callbackUrl, title, webhookType) { 
        
        ServiceWebhookAdd.initialize(this, callbackUrl, title, webhookType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, callbackUrl, title, webhookType) { 
        obj['callback_url'] = callbackUrl;
        obj['title'] = title;
        obj['webhook_type'] = webhookType;
    }

    /**
     * Constructs a <code>ServiceWebhookAdd</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ServiceWebhookAdd} obj Optional instance to populate.
     * @return {module:model/ServiceWebhookAdd} The populated <code>ServiceWebhookAdd</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ServiceWebhookAdd();

            if (data.hasOwnProperty('callback_url')) {
                obj['callback_url'] = ApiClient.convertToType(data['callback_url'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('webhook_type')) {
                obj['webhook_type'] = ApiClient.convertToType(data['webhook_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ServiceWebhookAdd</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ServiceWebhookAdd</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ServiceWebhookAdd.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['callback_url'] && !(typeof data['callback_url'] === 'string' || data['callback_url'] instanceof String)) {
            throw new Error("Expected the field `callback_url` to be a primitive type in the JSON string but got " + data['callback_url']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['webhook_type'] && !(typeof data['webhook_type'] === 'string' || data['webhook_type'] instanceof String)) {
            throw new Error("Expected the field `webhook_type` to be a primitive type in the JSON string but got " + data['webhook_type']);
        }

        return true;
    }


}

ServiceWebhookAdd.RequiredProperties = ["callback_url", "title", "webhook_type"];

/**
 * @member {String} callback_url
 */
ServiceWebhookAdd.prototype['callback_url'] = undefined;

/**
 * @member {String} title
 */
ServiceWebhookAdd.prototype['title'] = undefined;

/**
 * @member {module:model/ServiceWebhookAdd.WebhookTypeEnum} webhook_type
 */
ServiceWebhookAdd.prototype['webhook_type'] = undefined;





/**
 * Allowed values for the <code>webhook_type</code> property.
 * @enum {String}
 * @readonly
 */
ServiceWebhookAdd['WebhookTypeEnum'] = {

    /**
     * value: "messaging"
     * @const
     */
    "messaging": "messaging",

    /**
     * value: "purchase_number"
     * @const
     */
    "purchase_number": "purchase_number"
};



export default ServiceWebhookAdd;

